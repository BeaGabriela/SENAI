constraint pk_itens_sol primary key(Num_Sol, cod_produto), --Significa que os dois são chaves primarias. Usando pk para definir isso.

npm i le o packegae jsn e instala as dependencias lida np package  json.